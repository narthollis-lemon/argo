apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - resources/apps.application.yaml # bootstrap apps should self manage
  - resources/apps.applicationset.yaml # applicationset that uses git generator to create the apps apps
  - resources/apps.appproject.yaml

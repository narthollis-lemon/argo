apiVersion: v1
kind: Service
metadata:
  name: lidarr
spec:
  selector:
    app.kubernetes.io/name: downloaders
    app.kubernetes.io/component: lidarr
    app.kubernetes.io/part-of: media
  ports:
  - port: 8686
    targetPort: 8686
    name: http

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: lidarr
  namespace: media
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: media
    sectionName: https-gw
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /lidarr
    backendRefs:
    - group: ""
      kind: Service
      name: lidarr
      port: 8686
      weight: 1
    filters:
    - type: ExtensionRef
      extensionRef:
        group: traefik.io
        kind: Middleware
        name: local-network

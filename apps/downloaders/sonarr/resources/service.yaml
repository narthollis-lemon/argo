apiVersion: v1
kind: Service
metadata:
  name: sonarr
spec:
  selector:
    app.kubernetes.io/name: downloaders
    app.kubernetes.io/component: sonarr
    app.kubernetes.io/part-of: media
  ports:
  - port: 8989
    targetPort: 8989
    name: http
